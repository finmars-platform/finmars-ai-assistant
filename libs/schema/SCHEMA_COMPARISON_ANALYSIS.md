# DISCLAIMER

**!THIS FILE WAS GENERATED BY AI AGENT, MUST BE VALIDATED, PROVED!**

## What was done:
1. Generated `finmars_schema.py` from `libs/openapi/portfolio/openapi.json`
```bash
datamodel-codegen  --input ./libs/openapi/portfolio/openapi.json --input-file-type openapi --output ./libs/schema/via_data_model_codegen/finmars_schema.py --target-python-version 3.12 --output-model-type pydantic_v2.BaseModel
```
2. Generated via AI Agent Pydantic models: `libs/schema/__init__.py`
3. Cross-validated `finmars_schema.py` and `libs/schema/__init__.py` models


## How to use:
1. Use models in your code from `libs/schema/__init__.py`
2. But check/cross-validate manually just looking at `libs/schema/via_data_model_codegen/finmars_schema.py` to understand: is it the same? what's the difference?


## Cross-Validation Update Report

### Overview
A comprehensive cross-validation was performed between the manually created Pydantic models in `/libs/schema/` and the auto-generated schema in `/libs/schema/via_data_model_codegen/finmars_schema.py`. All models were updated to ensure consistency while preserving important metadata and constraints.

### Key Changes Applied

#### 1. Field Optionality Alignment
- **ID fields**: Changed from required to `Optional[int]` across all models
- **Timestamp fields**: `created_at`, `modified_at`, `deleted_at` changed to `Optional[datetime]`
- **Boolean flags**: Fields like `is_active`, `is_deleted`, `is_enabled` changed to `Optional[bool]`
- **Default-valued fields**: Fields with default values (empty strings, booleans) converted to Optional types

#### 2. Preserved Important Constraints
- **Readonly fields**: Maintained all `description="readonly"` annotations
- **String length constraints**: Kept `max_length=255` for string fields
- **Required field constraints**: Preserved `min_length=1` for required string fields
- **Numeric constraints**: Maintained `ge`, `le` constraints for integer fields

#### 3. Updated Models by File

**account.py**
- `AccountTypeView`: id changed to Optional
- `AccountView`: id changed to Optional

**base.py**
- No changes required - enums and base models were already correct

**client.py**
- `ClientSecretLight`: id and client fields changed to Optional
- `Clients`: id changed to Optional, list fields changed to Optional[List[...]]

**common.py**
- Multiple classifier models: id, level, parent fields changed to Optional
- `GenericAttributeType/View`: Various fields changed to Optional
- `RecalculateAttributes`: All fields changed to Optional[str]

**counterparty.py**
- Both models: id fields changed to Optional

**currency.py**
- `CurrencyView`: id changed to Optional

**instrument.py**
- `InstrumentClass`: name and description changed to Optional
- `InstrumentTypeView`: id changed to Optional
- `InstrumentView`: id and multiple boolean/dict fields changed to Optional

**portfolio.py** (largest update)
- All models updated with Optional fields for ids, timestamps, and default-valued fields
- Special handling for fields with `min_length=1` constraint preserved
- List fields changed from `List[T] = Field([])` to `Optional[List[T]] = Field(None)`

**pricing.py**
- `PricingPolicy`: id and all default-valued fields changed to Optional

**reconcile.py**
- `Params`: All fields changed to Optional
- Other models: id and timestamp fields changed to Optional

**responsible.py**
- Both models: id fields changed to Optional

**transaction.py**
- `TransactionClass`: name and description changed to Optional
- `TransactionTypeView`: id and all user_* fields (55 fields) changed to Optional

**responses.py**
- No changes required - response wrapper models were already correct

### Result
All 13 model files have been successfully updated. The schema now maintains consistency with the auto-generated version while preserving:
- Field descriptions and documentation
- Validation constraints (max_length, min_length, ge, le)
- Readonly field indicators
- Proper type hints and imports

The models are now ready for use with improved flexibility for optional fields while maintaining data integrity through preserved constraints.

